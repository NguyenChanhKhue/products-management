mixin search(keyword)
  form(id="form_search")
    .input-group
      input.form-control(
        type="text" 
        placeholder = "Nhập từ khoá"
        name = "keyword"
        value=keyword
        )
      .input-group-append
        button(
          class="btn btn-success"
          type="submit"
        ) Tìm kiếm