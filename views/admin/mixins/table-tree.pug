//- tạo danh mục cha con 

mixin table-tree(items, level, counterObj = {count: 0})
  each item, index in items 
    - const prefix = Array(level + 1).join(" -- ")
    tr 
      td #{counterObj.count + 1}
      - counterObj.count++
      td
        img(src=item.thumbnail
        alt=item.title
        width="100px"
        height="auto"
        )
        
      td #{prefix}#{item.title}
      td
        input(
          type="number"
          value= item.position
          style="width: 60px"
          min = 1
          name="position"
          )
      td 
        if(item.status == "active")
          a( 
            href="javascript:;" 
            data-status=item.status
            data-id = item._id
            button-change-status
            class="badge badge-success"

            )  Hoạt động
        else 
          a(
            href="javascript:;"
            data-status=item.status
            data-id = item._id
            button-change-status
            class="badge badge-danger"
            ) Dừng hoạt động 
        
      td
        a(
          href=`${prefixAdmin}/product-categories/detail/${item._id}`
          class="btn btn-secondary"
          style="margin-right:10px"                 
          ) Chi tiết
        a(
          href=`${prefixAdmin}/product-categories/edit/${item._id}`
          class="btn btn-warning"
          style="margin-right:10px"                 
          ) Sửa
        button(class="btn btn-danger" 
              button-delete
              data-id = item._id) Xoá
    if item.children && item.children.length > 0
      +table-tree(item.children, level + 1, counterObj)